/*
Roober Cruz
September 13, 2018
This program takes a use-inputted number from 1-10 and displays it in roman numeral
*/

#include <iostream>

using namespace std;

int main()
{
    cout.setf(ios::fixed);
    cout.setf(ios::showpoint);
    cout.precision(2);
    int c = 100;

    for(int c = 100; c <= 100; c--)
    {
        double weirdThing = (9.00/5.00);
        double fahr = ((c * weirdThing) + 32);
         if(c == fahr)
       {
           cout << "Value: " << c << endl;
           break;
       }
    }

    return 0;
}
